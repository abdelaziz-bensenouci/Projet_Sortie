<form method="post" action="{{ path('app_sortie_supprimer', {'id': sortie.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette sortie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sortie.id) }}">
    <button class="btn_supprimer_sortie2">Supprimer</button>
</form>
