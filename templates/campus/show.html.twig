{% extends 'base.html.twig' %}

{% block title %}Campus{% endblock %}

{% block body %}
    <fieldset class="fieldset">
    <legend><h1>Campus</h1></legend>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ campus.nom }}</td>
            </tr>
        </tbody>
    </table>
        <br>
    {{ include('campus/_delete_form.html.twig') }}
    </fieldset>
    <br>
    <a href="{{ path('app_campus_index') }}" class="btn">Retour à la liste</a>




{% endblock %}
